this takes the dataset in the csv, creates a classification model, and uses few-shot to help in the model training. just run train.py in "backend/models/train.py" to train the model

i have used ollama and gpt-oss:20b to for the LLM capabilities that is exposed in pipeline.py, exposed to the frontend via api.py
